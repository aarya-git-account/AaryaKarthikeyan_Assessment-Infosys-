BEGIN
    DBMS_SCHEDULER.CREATE_JOB (
        JOB_NAME        => 'EMPLOYEE_WORK_HOURS_WEEKLY_REPORT_JOB',  
        JOB_TYPE        => 'PLSQL_BLOCK',              
        JOB_ACTION      => 'BEGIN GENERATE_TOTAL_HOURS_REPORT; END;',  
        START_DATE      => SYSTIMESTAMP,               
        REPEAT_INTERVAL => 'FREQ=WEEKLY; BYDAY=MON; BYHOUR=8', 
        ENABLED         => TRUE,                       
        COMMENTS        => 'GENERATES TOTAL EMPLOYEE WORK HOURS REPORT WEEKLY'  
    );
    
    -- CONFIRMATION MESSAGE
    DBMS_OUTPUT.PUT_LINE('EMPLOYEE WORK HOUR WEEKLY REPORT CREATED SUCCESSFULLY');
    
EXCEPTION
    -- ERROR HANDLING SECTION
    WHEN OTHERS THEN
        DBMS_OUTPUT.PUT_LINE('ERROR CREATING JOB: ' || SQLERRM);
END;
/
